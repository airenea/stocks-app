hiiii this is the transaction page
<br />

<ul>
  <% @owned_stocks.each do |s| %>
    <% stock_id = s.stock_id %>
    <li>
      <%= s.stock_id %> (<%=@stock.where(id: stock_id)[0].symbol%>)
    </li>
  <% end %>
</ul>